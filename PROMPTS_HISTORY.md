# HelpDesk Lite — Original Plans History

> These are the **actual plans** generated by Claude, recovered from `~/.claude/plans/`.
> Original prompts (what the user typed) are not persisted — only the plans Claude generated in response.
> Plans are ordered chronologically by build phase.

---

## Plan 1 — Project Scaffold

**File:** `how-to-create-react-serialized-hejlsberg.md`

# Plan: Scaffold a production-grade TypeScript React project

## Context

The user wants a new React project created at `/home/bero/work/` that is:

- TypeScript-based
- Equipped with the libraries a typical **production** React app ships with

This is a fresh scaffold (no existing code to integrate). Stack decisions confirmed with the user:

- **Build tool:** Vite (CRA is deprecated)
- **Styling:** Tailwind CSS
- **State:** Redux Toolkit
- **Folder name:** `help_desk_lite` (normalized from "help desk lite" — npm names can't contain spaces; matches snake_case convention of sibling projects in `work/`)

Target path: `/home/bero/work/help_desk_lite`

## Stack / Libraries

**Core**

- `react`, `react-dom` (via Vite `react-ts` template)
- TypeScript

**Routing**

- `react-router-dom`

**Server state / data fetching**

- `@tanstack/react-query`
- `axios`

**Client/global state**

- `@reduxjs/toolkit`, `react-redux`

**Styling**

- `tailwindcss`, `postcss`, `autoprefixer`
- `clsx` + `tailwind-merge` (className composition)

**Forms & validation**

- `react-hook-form`, `zod`, `@hookform/resolvers`

**Tooling / quality**

- ESLint (Vite default flat config) + `eslint-plugin-react-hooks`, `eslint-plugin-react-refresh`
- Prettier + `prettier-plugin-tailwindcss`
- Husky + lint-staged (pre-commit hook)

**Testing**

- `vitest`, `@testing-library/react`, `@testing-library/jest-dom`, `@testing-library/user-event`, `jsdom`

## Build sequence

1. Scaffold: `cd /home/bero/work && npm create vite@latest help_desk_lite -- --template react-ts`
2. `cd help_desk_lite && npm install`
3. Install runtime deps:
   `npm i react-router-dom @tanstack/react-query axios @reduxjs/toolkit react-redux react-hook-form zod @hookform/resolvers clsx tailwind-merge`
4. Install dev deps:
   `npm i -D tailwindcss postcss autoprefixer prettier prettier-plugin-tailwindcss husky lint-staged vitest @testing-library/react @testing-library/jest-dom @testing-library/user-event jsdom`
5. Init Tailwind: `npx tailwindcss init -p`, set `content` globs, add `@tailwind base/components/utilities` to `src/index.css`.
6. Add project structure (see below) and wire providers in `src/main.tsx`:
   - `<Provider store>` (Redux) → `<QueryClientProvider>` → `<BrowserRouter>` → `<App/>`
7. Config files:
   - `.prettierrc` (with tailwind plugin), `.prettierignore`
   - `vitest.config.ts` + `src/test/setup.ts` (jest-dom)
   - `npm run test` script, `husky` pre-commit running `lint-staged`
8. Verify (see Verification).

## Proposed folder structure

```
src/
  app/            # store.ts, hooks.ts (typed useAppDispatch/useAppSelector)
  components/     # shared UI
  features/       # redux slices + feature UI
  lib/            # axios instance, queryClient, cn() util
  pages/          # route components
  routes/         # router config
  test/           # setup.ts
  main.tsx
  App.tsx
```

## Verification

- `npm run dev` → app boots at http://localhost:5173 with a styled (Tailwind) sample page.
- `npm run build` → type-checks and produces a production bundle with no errors.
- `npm run test` → sample component test passes.
- `npm run lint` → no lint errors.
- Confirm Redux store, React Query devtools/provider, and a sample route all render.

---

## Plan 2 — Architecture Foundation

**File:** `pls-save-this-in-crispy-garden.md`

# HelpDesk Lite — Architecture Foundation Plan

## Context

HelpDesk Lite is a lightweight internal support ticketing web app (Jira/Frappe-Helpdesk-like), built in React. The user comes from a Flutter/Cubit/Clean-Architecture background and wants a **production-grade React architecture** established **before any feature is built**. This plan sets up the shared foundation (steps 1–10 the user defined) so that later features follow a fixed flow: `models → datasource → repo → repo_impl → injector → logic hook → ui`.

No feature code is generated in this plan — only the architecture skeleton + one example wiring of the shared pieces.

## Agreed stack decisions

- **Logic layer ("Cubit"):** Custom hooks + **TanStack React Query** (server state) + `useState`/Redux Toolkit (UI state). Already wired in `main.tsx`.
- **Dependency injection:** **tsyringe**, using **token + `useFactory` registration** (NOT `@injectable` decorators) to avoid `emitDecoratorMetadata`, which Vite/esbuild doesn't support without extra plugins. Keeps build config untouched.
- **Data layer:** **Firebase JS SDK** (Firestore/Auth) behind handlers → single core datasource → repos → React Query in hooks. **Remove axios** (unused).
- **Folder structure:** **Feature-Sliced Design** — `core/` for shared, `features/<name>/` for each feature with internal layers.
- **SOLID:** every service/datasource/repo gets an **abstract interface + impl class**.

## Existing scaffold (reuse, don't recreate)

- `src/main.tsx` — already provides Redux `<Provider>`, `<QueryClientProvider>`, `<BrowserRouter>`. Will add `<DependencyProvider>` (DI bootstrap) here.
- `src/lib/cn.ts` — Tailwind class merge helper (reuse in all widgets).
- `src/lib/queryClient.ts` — React Query client config (reuse).
- `src/app/store.ts`, `src/app/hooks.ts` — Redux store + typed hooks (keep for UI state).
- `src/routes/AppRoutes.tsx` — router (extend later).
- `tailwind.config.js` — empty theme; extend with design tokens.
- Remove the `src/features/counter/*` example slice once real UI state exists (keep for now as reference).

## Target folder structure

```
src/
  core/
    theme/         theme tokens (colors, typography, spacing) + Tailwind config bridge + AppThemeProvider
    widgets/       shared components: AppButton, AppTextField, AppDropdown, AppCard, etc.
    responsive/    breakpoints + useBreakpoint hook + Responsive/Show components
    errors/        AppError, FirebaseErrorMapper (auth + firestore -> friendly messages)
    firebase/      firebaseConfig + handlers: AuthHandler (abstract+impl), FirestoreHandler (abstract+impl)
    datasource/    AppDataSource (abstract) + AppDataSourceImpl  — single file of all Firebase send/receive
    di/            tokens.ts + injector.ts (tsyringe container registration) + DependencyProvider.tsx + useInjected hook
  features/
    <feature>/     (NOT built yet) models/ repo/ repo_impl/ logic/ ui/{views,widgets}
  app/  lib/  routes/  (existing)
```

## Build sequence (the 10 steps)

### 1. App theme (context-inherited defaults)

- `tailwind.config.js`: extend `theme.extend.colors` (primary, surface, danger, muted…), `fontFamily`, `borderRadius`, spacing — these become the static defaults every widget inherits via Tailwind.
- `src/core/theme/tokens.ts` — typed token objects (color/text/spacing) for use in TS where Tailwind classes don't reach.
- `src/core/theme/AppThemeProvider.tsx` — React context exposing tokens + (optional) light/dark; wraps app so `useTheme()` works without importing the theme class everywhere. Mirrors Flutter `Theme.of(context)`.
- Global base styles in `src/index.css` (font, body bg from tokens).

### 2. Common widgets

- `src/core/widgets/AppButton.tsx`, `AppTextField.tsx` (integrates React Hook Form + Zod), `AppDropdown.tsx`, plus `AppCard`, `AppBadge` as needed. One class/component per file; use `cn()` + theme tokens; fully prop-customizable; no business logic.

### 3. Responsive layer

- Use **Tailwind responsive breakpoints** as the base (trusted, production standard) plus a small `useBreakpoint()` hook in `src/core/responsive/` and `<Show above="md">` helper components. No heavy library needed — Tailwind covers it. (If a JS-driven need arises, `react-responsive` is the trusted option; flagged, not added now.)

### 4. App error (Firebase)

- `src/core/errors/AppError.ts` — normalized error type (code, message, cause).
- `src/core/errors/FirebaseErrorMapper.ts` — maps Firebase Auth + Firestore error codes to friendly `AppError`s. Used by handlers/datasource.

### 5. Firebase service handlers (abstract + impl)

- `src/core/firebase/firebaseConfig.ts` — init app (env vars via Vite `import.meta.env`).
- `AuthHandler` (interface) + `AuthHandlerImpl` — signIn/out, current user, auth state.
- `FirestoreHandler` (interface) + `FirestoreHandlerImpl` — typed get/add/update/delete/query, wraps errors via `FirebaseErrorMapper`.

### 6. Single core datasource

- `src/core/datasource/AppDataSource.ts` (interface) + `AppDataSourceImpl.ts` — the ONE file-set holding all Firebase send/receive, delegating to the handlers. Features call this, never Firebase directly. No per-feature datasources.

### 7. Injector (tsyringe)

- `src/core/di/tokens.ts` — `InjectionToken` symbols for AuthHandler, FirestoreHandler, AppDataSource, (later) repos.
- `src/core/di/injector.ts` — `container.register(token, { useFactory: ... })` wiring impls to interfaces (no decorators).
- `src/core/di/DependencyProvider.tsx` + `useInjected(token)` — expose container through React context; mount in `main.tsx`.
- Add `tsyringe` (+ `reflect-metadata` imported once at entry). No tsconfig/Vite build changes thanks to `useFactory`.

### 8 & 9. SOLID + abstract/impl

- Applied throughout: every handler/datasource/repo = interface + impl, depended on via tokens (Dependency Inversion). Reviewed per file as built.

### 10. Architecture rules file

- `.claude/CLAUDE.md` (project-local) documenting: feature flow, FSD folders, abstract+impl rule, single-core-datasource rule, no-Firebase-in-features, theme-inheritance, one-class-per-file, React Query for server state, tsyringe token registration — so future work can't drift.

## Dependency changes

- **Add:** `firebase`, `tsyringe`, `reflect-metadata`.
- **Remove:** `axios` (+ `src/lib/axios.ts`).
- **MCP:** Will **ask before using the Firebase MCP** to create/configure the Firebase project + fetch SDK config (`firebase_get_sdk_config`). Context7 MCP for current Firebase v9+ / React Query v5 / tsyringe docs.

## Verification

- `npm run build` (tsc + vite) passes with new core modules.
- `npm run lint` clean.
- `npm run test` — add a smoke test: resolve `AppDataSource` from the injector and assert it returns the impl; render `AppButton`/`AppTextField` via Testing Library.
- Manually `npm run dev` — app boots with DependencyProvider mounted, no console errors.

## Out of scope (explicitly NOT in this plan)

- Any feature (tickets, auth screens, dashboards). Only the shared foundation + one example wiring.
- Final Firebase data model / ticket states (still open questions in the BRD).

---

## Plan 3 — Teams, Epics, Sprints & Role-Based Hierarchy

**File:** `proud-knitting-sundae.md`

# Plan: Teams, Epics, Sprints & Role-Based Hierarchy

## Context

HelpDesk Lite currently has auth (with roles: pending/member/team_leader/manager/admin) and tickets (single assignee, 6-status workflow, subtasks as subcollection). We need to extend it with a manager→team-leader→member hierarchy, team management, scoped assignment, richer tickets (multi-assignee, embedded subtasks, labels, epic/sprint links), epics, and sprints with role-based visibility.

## Execution: 6 Sessions

---

### Session 1: Data Layer — Models & Schemas

All new/modified models so subsequent sessions can import them.

**Create:**

- `src/features/teams/models/Team.ts` — `Team` interface (id, label, managerId, teamLeaderId, memberCount, employees map, timestamps), `TeamEmployee` type ({name, photoUrl, role}), zod schemas, fromDoc/toDoc
- `src/features/epics/models/Epic.ts` — `Epic` interface (id, title, description, teamId, createdBy, color, timestamps), zod schemas, fromDoc/toDoc
- `src/features/sprints/models/Sprint.ts` — `Sprint` interface (id, name, startDate, endDate, teamId, createdBy, ticketIds[], assignedMemberIds[], timestamps), zod schemas, fromDoc/toDoc

**Modify:**

- `src/features/auth/models/AuthUser.ts` — Add `teamId: string | null` and `managerId: string | null` to `AuthUser` + update all mappers
- `src/features/tickets/models/Ticket.ts`:
  - Statuses: `['todo', 'inProgress', 'done']` (runtime migration in fromDoc: planning/design→todo, implementation/testing→inProgress, deployment/completed→done)
  - Priorities: `['low', 'medium', 'high', 'urgent']` (runtime migration: critical→urgent)
  - `assignedTo` → `assigneeIds: string[]` (fromDoc backward compat: if `assigneeIds` missing, use `[doc.assignedTo]`)
  - Add: `epicId: string | null`, `sprintId: string | null`, `labels: string[]`
  - Add embedded `subtasks: Subtask[]` where `Subtask = { id: string; title: string; assigneeId: string; isDone: boolean }`
  - Update zod schemas, statusLabels, fromDoc/toDoc
- `src/core/widgets/StatusBadge.tsx` — Update to 3 statuses (todo/inProgress/done)
- `src/core/widgets/PriorityBadge.tsx` — Update: critical→urgent
- Possibly deprecate `src/features/tickets/models/SubTicket.ts` (subtasks now embedded)

---

### Session 2: Teams Feature + Scoped Assignment

**Create (full feature slice):**

- `src/features/teams/repo/TeamRepo.ts` — interface: listTeams, getTeam, createTeam, updateTeam, deleteTeam, addMember, removeMember
- `src/features/teams/repo_impl/TeamRepoImpl.ts` — collection `'teams'`; addMember writes `employees.{userId}` + updates user's teamId; removeMember reverses
- `src/features/teams/logic/useTeamController.ts` — React Query hooks wrapping TeamRepo
- `src/features/teams/logic/useTeamMembersController.ts` — **scoped assignment helper**: given a teamId, returns member options from `team.employees` map (no full-users query)
- `src/features/teams/ui/views/TeamsListView.tsx`, `TeamCreateView.tsx`, `TeamDetailView.tsx`
- `src/features/teams/ui/widgets/TeamForm.tsx`, `TeamMemberList.tsx`

**Modify:**

- `src/core/di/tokens.ts` — Add `TeamRepo` token
- `src/core/di/injector.ts` — Register `TeamRepoImpl`
- `src/routes/AppRoutes.tsx` — Add `/teams`, `/teams/new`, `/teams/:id` (RoleGuard: manager, admin)
- `src/features/tickets/logic/useMembersController.ts` — Refactor to use team-scoped assignment

---

### Session 3: Tickets Rework — Multi-Assignee, Embedded Subtasks, Labels, Epic/Sprint Links

**Modify:**

- `src/features/tickets/repo/TicketRepo.ts` — `assignedTo`→`assigneeId` in filters (uses `array-contains`), add `epicId`/`sprintId` filters, remove subcollection subtask methods, add `addSubtask`/`removeSubtask`/`toggleSubtask` for embedded array
- `src/features/tickets/repo_impl/TicketRepoImpl.ts` — Rewrite queries for `array-contains` on assigneeIds, embedded subtask mutations via `arrayUnion`/read-modify-write, remove subtask subcollection paths
- `src/features/tickets/logic/useSubTicketController.ts` — Rewrite for embedded subtasks (no separate query, data comes from ticket doc)
- `src/features/tickets/logic/useTicketController.ts` — Update mutation inputs for new schema
- `src/features/tickets/ui/widgets/TicketForm.tsx` — Multi-select assignees (from useTeamMembersController), epic dropdown, sprint dropdown, labels input
- `src/features/tickets/ui/widgets/SubTicketForm.tsx`, `SubTicketList.tsx`, `SubTicketRow.tsx` — Simplify for embedded model (title + assigneeId + isDone toggle)
- `src/features/tickets/ui/widgets/TicketRow.tsx` — Multiple assignee display, new statuses
- `src/features/tickets/ui/widgets/TicketFilters.tsx` — Update status options
- `src/features/tickets/ui/views/TicketDetailView.tsx` — Epic/sprint links, labels, new subtask UI
- `src/features/tickets/ui/views/TicketCreateView.tsx` — Wire new fields
- `src/features/tickets/logic/ticketsSlice.ts` — Update statusFilter options

---

### Session 4: Epics Feature

**Create (full feature slice):**

- `src/features/epics/repo/EpicRepo.ts` — listEpics(teamId), getEpic, createEpic, updateEpic, deleteEpic
- `src/features/epics/repo_impl/EpicRepoImpl.ts` — collection `'epics'`, filter by teamId
- `src/features/epics/logic/useEpicController.ts` — standard React Query pattern
- `src/features/epics/ui/views/EpicsListView.tsx`, `EpicDetailView.tsx`
- `src/features/epics/ui/widgets/EpicForm.tsx`, `EpicCard.tsx`

**Modify:**

- `src/core/di/tokens.ts` — Add `EpicRepo` token
- `src/core/di/injector.ts` — Register `EpicRepoImpl`
- `src/routes/AppRoutes.tsx` — Add `/epics`, `/epics/new`, `/epics/:id` (RoleGuard: team_leader, manager, admin)

---

### Session 5: Sprints + Role-Based Visibility

**Create (full feature slice):**

- `src/features/sprints/repo/SprintRepo.ts` — listSprints, getSprint, createSprint, updateSprint, deleteSprint, addTicketToSprint, removeTicketFromSprint
- `src/features/sprints/repo_impl/SprintRepoImpl.ts` — addTicketToSprint: `arrayUnion` on sprint.ticketIds + set ticket.sprintId + recompute assignedMemberIds; removeTicketFromSprint: reverse
- `src/features/sprints/logic/useSprintController.ts` — visibility logic based on user role:
  - Manager/admin: all sprints
  - Team leader: `where('teamId', '==', user.teamId)`
  - Member: `where('assignedMemberIds', 'array-contains', user.uid)`
- `src/features/sprints/ui/views/SprintsListView.tsx`, `SprintDetailView.tsx`
- `src/features/sprints/ui/widgets/SprintForm.tsx`, `SprintBoard.tsx` (kanban: todo/inProgress/done columns)

**Modify:**

- `src/core/di/tokens.ts` — Add `SprintRepo` token
- `src/core/di/injector.ts` — Register `SprintRepoImpl`
- `src/routes/AppRoutes.tsx` — Add `/sprints`, `/sprints/new`, `/sprints/:id`

---

### Session 6: Firestore Security Rules

**Modify `firestore.rules`** — comprehensive rules for all collections:

- Helper functions: `isAdmin()`, `isManager()`, `isTeamMember(teamId)`, `isTeamLeader(teamId)`
- `users/{userId}` — own doc read/write; admin full access; team_leader read for assignment
- `teams/{teamId}` — manager/admin write; team members read
- `tickets/{ticketId}` — team members read; team_leader/manager/admin of that team write
- `tickets/{ticketId}/activity/{activityId}` — team members read + create comments
- `epics/{epicId}` — scoped to teamId, same pattern as tickets
- `sprints/{sprintId}` — team_leader/manager/admin write; members read if in assignedMemberIds

---

## Key Design Decisions

1. **No batch Firestore migration** — runtime converters in `fromDoc()` handle old→new format lazily
2. **No Cloud Functions** — denormalized fields synced client-side in repo_impl (acceptable for MVP)
3. **Subtasks embedded** (not subcollection) — small N, edited together, simpler queries
4. **Comments stay as subcollection** — can grow large, needs pagination
5. **Scoped assignment from employees map** — no full-users query, O(1) lookup
6. **`arrayUnion`/`arrayRemove`** passed through AppDataSource's `updateDocument` (Firestore FieldValues pass as `unknown`)

## Verification

- `npm run build` — no TS errors after each session
- `npm run lint` — clean
- Manual testing: create team → assign leader/members → leader creates epic → creates tickets with multi-assignee + subtasks → creates sprint → adds tickets → verify member sees only their sprints
- Firestore rules: test with Firebase emulator or rules playground

---

## Plan 4 — Jira-Style Agile Board & Ticket Upgrade

**File:** `objective-extend-the-admin-reactive-castle.md`

# Jira-Style Agile Board & Ticket Upgrade — Implementation Plan

## Context

HelpDesk Lite already has a working vertical: rigid role-based auth, tickets,
sprints, epics, teams, a 3-column (static) sprint board, and basic team-progress
analytics. The ask is to upgrade it into a Jira-style agile tool: **dynamic
permissions & admin-managed statuses/labels**, a **much richer Ticket** (rich-text
description, ticketScore, linked tickets, threaded/reactable comments, richer
subtasks), and new **Backlog / Board / Timeline / Summary / Profile** views with
drag-and-drop, i18n, dark mode, and a "GenZ luxury" feel.

Decisions locked with the user:

- **Permissions:** _additive_ — keep the `ROLES` enum working, add a
  `Role -> permissions[]` mapping + admin-creatable custom roles. Guards check
  permissions derived from role by default. Nothing existing breaks.
- **Libraries approved:** `@dnd-kit/*`, `@tiptap/*`, `react-i18next` (+RTL),
  `lottie-react`.
- **Delivery:** one plan, executed **phased** with a review checkpoint after each
  of the 4 steps.
- **Schema:** **back-compat migration** — add `endDate` (mappers keep reading old
  `deadline`), `status` becomes a dynamic string id with the 3 current statuses
  seeded as defaults; old docs keep rendering via mapper fallbacks.

Build order is non-negotiable (project rule):
`models -> datasource -> repo -> repo_impl -> injector -> logic -> ui`. Every
service/repo is an interface + impl pair registered by token in
`src/core/di/injector.ts`. All Firebase access stays behind `AppDataSource`.
No `@injectable` decorators. Tailwind theme tokens only.

---

## Library setup (one-time, start of Step 1)

Add deps and wire providers:

- `@dnd-kit/core`, `@dnd-kit/sortable`, `@dnd-kit/utilities`
- `@tiptap/react`, `@tiptap/starter-kit`, `@tiptap/extension-link`
- `react-i18next`, `i18next`
- `lottie-react`

Wiring:

- i18n: `src/lib/i18n.ts` (init, en/ar resource bundles, `dir` switch) imported in
  `src/main.tsx`. Locale files `src/lib/locales/{en,ar}.json`.
- Dark mode: set `darkMode: 'class'` in `tailwind.config.js`; add `dark:` token
  variants in `src/core/theme/tokens.ts` + tailwind colors; toggle a `dark` class
  - `dir` attr on `<html>` from a new preferences slice.

---

## Step 1 — Domain models, schemas & datasource (data layer)

### New core: admin-managed config (dynamic statuses, labels, permissions)

- **Models** `src/features/admin/models/`:
  - `Permission.ts` — `PERMISSIONS` const, `Permission` type, and a default `ROLE_PERMISSIONS: Record<Role, Permission[]>` map
  - `CustomRole.ts` — `{ id, name, permissions: Permission[] }` + zod + mappers. Stored in Firestore `roles/{id}`.
  - `WorkflowStatus.ts` — `{ id, name, color, order }` + zod + mappers. Stored in `statuses/{id}`. Seed defaults To Do / In Progress / Blocked / Done.
  - `Label.ts` — `{ id, name, color }` + zod + mappers. Stored in `labels/{id}`.

### Auth model extension (additive)

- `AuthUser.ts`: add optional `customRoleId?: string | null` and a derived `permissions` helper. Keep `role` field intact.

### Ticket model upgrade (`src/features/tickets/models/Ticket.ts`)

- Change `status: TicketStatus` (enum) -> `statusId: string` (dynamic). Keep a legacy mapper.
- Add: `ticketScore: number`, `endDate: number`, `linkedTickets: TicketLink[]`
- `description` now holds Tiptap HTML/JSON
- `labels: string[]` now holds Label ids
- Keep `assigneeIds: string[]`

### Subtask upgrade

- Enrich inline `Subtask` to: `{ id, title, priority, assigneeId, statusId, startDate, endDate }`

### Comments upgrade (`models/TicketActivity.ts`)

- Extend: `{ ..., authorName, editedAt?, isEditable, replies: Reply[], reactions: Reaction[] }`

### Sprint & Epic

- `Sprint.ts`: add `sprintGoal: string` + `status?: 'planned'|'active'|'completed'`
- `Epic.ts`: add `startDate?`, `endDate?` for timeline view

**Checkpoint 1:** `npm run build` + `npm run lint` clean.

---

## Step 2 — Repos, DI tokens & logic hooks

### New repos

- `AdminConfigRepo` (`src/features/admin/repo` + `repo_impl`): CRUD for statuses, labels, custom roles. Token `TOKENS.AdminConfig`.
- Extend `AuthRepo`/`AuthRepoImpl`: `setUserCustomRole(uid, roleId)`, `listCustomRoles()`

### Repo method additions

- `TicketRepo`: `updateTicketStatus(id, statusId)`, `addLink`/`removeLink`, `addReply`, `editComment`, `toggleReaction`, richer subtask CRUD
- `SprintRepo`: support `sprintGoal`/`status`
- `EpicRepo`: timeline date fields

### DI

- Add `AdminConfig` token; register `AdminConfigRepoImpl`

### Logic hooks

- `useAdminConfigController` — statuses/labels/roles queries + mutations
- `usePermissions` — resolves current user's effective permissions into a `can(permission)` helper
- `useBoardController` (new) — group tickets by `statusId` for a team/sprint; `moveTicketStatus` mutation (optimistic update)
- `useBacklogController` (new) — backlog (no sprint) + per-sprint ticket lists + search/filter state
- `useSummaryController` (new) — completion rates, story-point rollups per team
- `usePreferences` slice (Redux) — theme mode, language, custom accent color, persisted to `localStorage` + user doc

**Checkpoint 2:** build + lint + `npm run test` clean.

---

## Step 3 — Reusable widgets

All in `src/core/widgets/` or feature `ui/widgets/`, one component per file, Tailwind theme tokens only, dark-mode aware, strings via `t()`.

- `RichTextEditor.tsx` — Tiptap wrapper (toolbar: bold/italic/link/code)
- `RichTextViewer.tsx` for read-only render
- `DraggableCard.tsx`, `DroppableColumn.tsx`, `DndBoardProvider.tsx` (dnd-kit context + sensors)
- `KanbanColumn.tsx` — themed column (header w/ status color + count, body)
- `AvatarStack.tsx` — overlapping assignee avatars w/ "+N" overflow
- `SearchFilterBar.tsx` — shared search input + filter dropdowns
- `EmptyState.tsx` / `SuccessCheck.tsx` — Lottie-backed states
- `StatusBadge` / `PriorityBadge` — refactor to accept dynamic status (id+color) from config
- `ColorPicker.tsx`, `LanguageSwitcher.tsx`, `ThemeToggle.tsx` for Profile
- `CommentThread.tsx` (replies + emoji reactions)

**Checkpoint 3:** build + lint clean; widgets render in isolation.

---

## Step 4 — Views assembly & routing

New views:

- `BacklogView` (`src/features/sprints/ui/views/BacklogView.tsx`) — sprints listed top (each a droppable), backlog list below. Drag tickets backlog <-> sprints.
- `BoardView` (`src/features/tickets/ui/views/BoardView.tsx`) — kanban columns from dynamic statuses; drag between columns -> `moveTicketStatus`
- `TimelineView` — Gantt-style: epics & tickets as bars spanning start->end; CSS-grid based
- `SummaryView` — team stats, completion rate, story-point rollup; simple themed bar/donut with divs/SVG
- `ProfileSettingsView` — personal data + preferences (dark/light, en/ar, accent color)
- Admin: `AdminRolesView`, `AdminWorkflowView` — create custom roles via permission checkboxes; manage statuses & labels

Routing & guards:

- Add routes: `/backlog`, `/board`, `/timeline`, `/summary`, `/profile`, `/admin/roles`, `/admin/workflow`
- Add `PermissionGuard` using `usePermissions`. Keep `RoleGuard` for unchanged routes (additive).
- Extend `AppSidebar` with new nav entries gated by permissions

**Checkpoint 4 / Definition of done:** `npm run build` && `npm run lint` && `npm run test` all pass.

---

## Plan 5 — Design System Redesign (Minimal Luxury)

**File:** `lets-start-impelementation-abundant-bonbon.md`

# Minimal-Luxury UI/UX Redesign — Auth + Design System

## Context

The Auth module is built and functional, but the visual language is generic (bright
`#2563eb` blue, system Inter, flat shadows) and the `design-system/helpdesk-lite/MASTER.md`
spec has drifted from the code. The goal is a cohesive **minimal + luxury**
aesthetic — **deep navy (#14213D) + champagne gold (#C9A86A)** accent, **Plus Jakarta Sans**
typography — applied token-first so every widget and screen inherits it.

Decisions confirmed with user:

- Palette: deep navy primary + champagne-gold accent, cool off-white surfaces.
- Type: **Plus Jakarta Sans** (headings 600–700 tracking-tight, body 400–500, uppercase micro-labels)
- Scope: full design system (tokens + tailwind + fonts) → all 5 core widgets → all 4 auth views + 3 auth widgets → documented in `design-system/`

Principle: **token-first**. Change `tailwind.config.js` + `tokens.ts` once; widgets already consume semantic classes (`bg-primary`, `text-content-muted`, `rounded-card`, `shadow-card`), so most screens upgrade automatically.

## 1. Fonts — load Plus Jakarta Sans

- **`index.html`** — add Google Fonts preconnect + link for Plus Jakarta Sans wght@300;400;500;600;700;800
- **`src/index.css`** — set `html { font-family: 'Plus Jakarta Sans', system-ui, sans-serif; }`, add `-webkit-font-smoothing: antialiased; text-rendering: optimizeLegibility;`

## 2. Design tokens

### `tailwind.config.js` — replace the `extend` block

```js
colors: {
  primary:  { DEFAULT:'#14213D', hover:'#0E1730', subtle:'#EEF1F6', foreground:'#FFFFFF' },
  accent:   { DEFAULT:'#C9A86A', hover:'#B8975A', subtle:'#F6F1E7', foreground:'#1A1408' },
  surface:  { DEFAULT:'#FFFFFF', muted:'#F1F2F5', border:'#E6E8EC' },
  canvas:   '#F7F8FA',
  content:  { DEFAULT:'#0B1220', muted:'#5B6573', subtle:'#95A0B0' },
  danger:   { DEFAULT:'#B4232A', subtle:'#FBEFEF', foreground:'#FFFFFF' },
  success:  { DEFAULT:'#2F6E4F', subtle:'#EDF4F0', foreground:'#FFFFFF' },
  warning:  { DEFAULT:'#9A7B3F', subtle:'#F7F1E6', foreground:'#FFFFFF' },
  info:     { DEFAULT:'#2C5A7A', subtle:'#EAF1F6', foreground:'#FFFFFF' },
},
fontFamily: { sans: ['Plus Jakarta Sans','system-ui','sans-serif'] },
letterSpacing: { tightest:'-0.03em', tight:'-0.02em', label:'0.10em' },
borderRadius: { control:'0.625rem', card:'1rem', pill:'9999px' },
boxShadow: {
  card:     '0 1px 2px rgb(11 18 32 / 0.04), 0 6px 20px -6px rgb(11 18 32 / 0.08)',
  elevated: '0 16px 48px -12px rgb(11 18 32 / 0.18)',
  focus:    '0 0 0 3px rgb(20 33 61 / 0.12)',
},
```

## 3. Core widgets — refine shape, spacing, motion

- **AppButton.tsx** — taller premium sizing (`sm h-9 / md h-11 / lg h-12`), `rounded-control`, `font-semibold tracking-tight`, new `accent` variant (gold CTA)
- **AppTextField.tsx** — `h-11`, `rounded-control`, label as **uppercase micro-label** (`text-xs font-semibold uppercase tracking-label text-content-muted`)
- **AppDropdown.tsx** — match AppTextField (height, radius, label style, focus)
- **AppCard.tsx** — `rounded-card`, `border-surface-border`, `bg-surface`, `p-6`, `shadow-card`
- **AppBadge.tsx** — `rounded-pill`, `tracking-tight`, add `accent` tone (gold)

## 4. Auth screens

- **widgets/AuthLayout.tsx** — flagship split-screen: Left navy brand panel with gold rule, Right surface form panel. Mobile: single centered column on `bg-canvas`
- **views/LoginView.tsx** — Plus Jakarta Sans heading, gold "Forgot password?" link, primary navy submit
- **views/ForgotPasswordView.tsx** & **ResetPasswordView.tsx** — same type hierarchy; muted success/error states
- **views/AdminUsersView.tsx** — uppercase micro-eyebrow + bold title; table with tracking-label headers, hairline dividers, hover row tint
- **widgets/RoleAssignmentModal.tsx** — `shadow-elevated`, `rounded-card`, backdrop `bg-primary/30 backdrop-blur-sm`
- **widgets/RoleBadge.tsx** — admin → `accent`/gold (premium feel)

## 5. Document in `design-system/helpdesk-lite/`

- **Rewrite `MASTER.md`** — real shipped system: navy + champagne-gold palette table, Plus Jakarta Sans type scale, radius/shadow/spacing tokens, anti-patterns list, a11y/contrast checklist
- **Add `tokens.md`** — exhaustive token reference mirroring `tailwind.config.js` + `tokens.ts`
- **Add `components.md`** — per-widget spec: variants, sizes, states

## Critical files

- `index.html`, `src/index.css` — fonts + base
- `tailwind.config.js`, `src/core/theme/tokens.ts`, `src/core/theme/AppThemeProvider.tsx` — tokens
- `src/core/widgets/{AppButton,AppTextField,AppDropdown,AppCard,AppBadge}.tsx` — widgets
- `src/features/auth/ui/widgets/{AuthLayout,RoleAssignmentModal,RoleBadge}.tsx`
- `design-system/helpdesk-lite/{MASTER.md,tokens.md,components.md}`

---

## Plan 6 — Permission Categories (Model Layer Addition)

**File:** `look-i-discover-something-shimmering-blanket.md`

# Context

The UI renders permission groups to show/hide sections based on a user's grants. Currently `Permission.ts` only has a flat `PERMISSIONS` array and inline comments for grouping. There is no machine-readable category structure the UI can consume. We need to add:

1. `PERMISSION_LABELS` — human-readable display name per key (for rendering checkboxes/lists)
2. `PERMISSION_CATEGORIES` — ordered list of category groups the UI iterates over to build section headers and permission rows

No new files, no new layers — pure model-layer addition to the existing `Permission.ts`.

## Files to modify

**`src/features/admin/models/Permission.ts`** — append two exports at the bottom.

## What to add

### 1. `PERMISSION_LABELS: Record<Permission, string>`

```ts
export const PERMISSION_LABELS: Record<Permission, string> = {
  can_invite_user: 'Invite user (by email)',
  can_remove_user: 'Remove user',
  // ... all 138 keys
};
```

### 2. `PermissionCategory` interface + `PERMISSION_CATEGORIES` array

```ts
export interface PermissionCategory {
  id: string;
  label: string;
  permissions: Permission[];
}

export const PERMISSION_CATEGORIES: PermissionCategory[] = [ ... ];
```

**Category order and membership** (22 categories, no duplicates):

| #     | id                     | label                                               | Permissions                                                                                                      |
| ----- | ---------------------- | --------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------- |
| 1     | `user-role-management` | User & Role Management                              | `can_invite_user`, `can_remove_user`, `can_assign_role`, `can_remove_role`, `can_set_custom_role_limits`         |
| 2     | `workflow-management`  | Workflow Management                                 | `can_create_workflow`, `can_edit_workflow`, `can_remove_workflow`                                                |
| 3     | `tickets-system`       | Tickets — System-wide                               | `can_create_ticket`, `can_view_all_tickets`, `can_edit_all_tickets`, `can_delete_all_tickets`, `can_move_ticket` |
| 4–8   | `tickets-custom-*`     | Tickets — Custom Team/Epic/Label/Workflow/User      | scoped view/edit/delete variants                                                                                 |
| 9     | `tickets-my-tasks`     | Tickets — My Tasks                                  | `can_view_own_tickets`, `can_view_own_subtasks`                                                                  |
| 10    | `tickets-assigned`     | Tickets — Assigned to Me                            | 12 permissions                                                                                                   |
| 11    | `comments-reactions`   | Comments & Reactions                                | 7 permissions                                                                                                    |
| 12    | `board-backlog`        | Board & Backlog                                     | 4 permissions                                                                                                    |
| 13–14 | `sprints-*`            | Sprints — System-wide / Custom Team                 | 8 permissions                                                                                                    |
| 15    | `epics`                | Epics                                               | 9 permissions                                                                                                    |
| 16    | `timeline`             | Timeline                                            | `can_view_timeline`                                                                                              |
| 17–19 | `subtasks-*`           | Subtasks — System-wide / Custom Team / Custom Label | 6 permissions                                                                                                    |
| 20–21 | `own-team-*`           | Own Team Tickets / Own Team Management              | 32 permissions total                                                                                             |
| 22    | `settings-admin`       | Settings & Admin                                    | 5 permissions                                                                                                    |

---

## Plan 7 — Skills Description Improvement

**File:** `continue-velvet-kazoo.md`

# Plan: Improve HelpDesk Lite skill descriptions for reliable triggering

## Context

Four project skills were created for HelpDesk Lite (helpdesk-feature, helpdesk-backend, helpdesk-frontend, helpdesk-uiux). The automated `run_loop.py` description optimizer failed. The pilot eval run revealed that `helpdesk-feature` has 0% recall — no positive queries triggered it — meaning all four descriptions are too passive and won't fire reliably on real Jira tickets.

## Files to modify

- `.claude/skills/helpdesk-feature/SKILL.md`
- `.claude/skills/helpdesk-backend/SKILL.md`
- `.claude/skills/helpdesk-frontend/SKILL.md`
- `.claude/skills/helpdesk-uiux/SKILL.md`

## New descriptions

### helpdesk-feature

```
Use FIRST whenever a Jira/epic ticket arrives that touches more than one layer
(model, repo, hook, view), or when asked to "implement a feature end-to-end",
"build the full slice", "what's the build order?", or "walk me through this card".
Routes to the correct layer skill and enforces the vertical build order:
models → datasource → repo → repo_impl → injector → logic → ui.
Do NOT use for single-layer tasks (styling tweak, one hook, one model).
```

### helpdesk-backend

```
Use for backend-epic tickets: creating or updating models (zod schema + fromDoc/toDoc
mappers), repo interfaces, repo_impl classes that call AppDataSource, registering DI
tokens in injector.ts, and adding new Firebase handlers (Storage, etc.).
Triggers on: "add a repo", "create a model", "register a token", "extend AppDataSource".
Do NOT use for hooks (useXxxController), views, styling, or routing.
```

### helpdesk-frontend

```
Use for frontend-epic tickets: building useXxxController hooks with React Query,
wiring react-hook-form + zodResolver, adding/updating routes in AppRoutes, Redux
UI-state slices, and cache invalidation logic.
Triggers on: "build the controller hook", "wire the form", "add a route", "useXxxController".
Do NOT use for models/repos (backend) or views/widgets/styling (ui/ux).
```

### helpdesk-uiux

```
Use for ui/ux-epic tickets: building views (loading/error/empty/data states), creating
or extending shared themed widgets in core/widgets, applying Tailwind theme tokens
(bg-primary, text-content-muted, rounded-card, shadow-card), and responsive layout.
Triggers on: "build the view", "design the card", "add a widget", "style with tokens".
Do NOT use for hooks, repos, models, routing, or cache logic.
```

---

## Plan 8 — Tickets Feature (Full Vertical)

**File:** `continue-sprightly-quail.md`

# Tickets Module — Full Vertical Implementation Plan

## Context

The user wants to implement the entire tickets module for HelpDesk Lite — tickets, sub-tickets, activity feeds, role-based access (member/team_leader/manager/admin), and deadline warnings. This covers ~60 SCRUM tickets across backend, frontend, and UI/UX epics.

**Files already created before plan mode** (on disk, kept):

- `src/features/tickets/models/Ticket.ts`
- `src/features/tickets/models/SubTicket.ts`
- `src/features/tickets/models/TicketActivity.ts`
- `src/features/tickets/repo/TicketRepo.ts`

## Build Order

### Phase 1: Backend Layer

1. **`src/features/tickets/repo_impl/TicketRepoImpl.ts`** — Implements `TicketRepo` using `AppDataSource`. Uses `where`/`orderBy`/`limit`/`startAfter` constraints. Soft-delete sets `isDeleted: true` + `deletedAt`. Sub-ticket paths: `tickets/{id}/subtasks`. Activity paths: `tickets/{id}/activity`.

2. **`src/core/di/tokens.ts`** — Add `TicketRepo` token

3. **`src/core/di/injector.ts`** — Register `TicketRepoImpl`

### Phase 2: Frontend Layer (Logic Hooks)

4. **`src/features/tickets/logic/useTicketController.ts`** — `ticketsQuery`, `ticketQuery(id)`, `createTicket`, `updateTicket`, `updateStatus`, `softDelete`
5. **`src/features/tickets/logic/useSubTicketController.ts`** — `subTicketsQuery(ticketId)`, `createSubTicket`, `updateSubTicketStatus`, `progress` derived value
6. **`src/features/tickets/logic/useActivityController.ts`** — `activityQuery(ticketId)`, `addComment`
7. **`src/features/tickets/logic/ticketsSlice.ts`** — Redux slice for UI-only state: active filters, search query, current page

### Phase 3: UI/UX Layer

#### Theme extensions

8. **`tailwind.config.js`** — Add status and priority colors
9. **`src/core/theme/tokens.ts`** — Add `statusColors` and `priorityColors` maps

#### Shared widgets

10. `StatusBadge.tsx` — status-specific tone mapping
11. `PriorityBadge.tsx` — priority-specific tone mapping
12. `DeadlineBadge.tsx` — yellow within 48h, red if overdue
13. `AppTextArea.tsx` — textarea variant of AppTextField
14. `AppDatePicker.tsx` — native date input with theme styling
15. `ConfirmDialog.tsx` — modal for discard/delete confirmations

#### Ticket feature widgets

16. `TicketRow.tsx` — row for ticket list
17. `TicketFilters.tsx` — status dropdown, priority dropdown, date range
18. `TicketSearchBar.tsx` — debounced text input
19. `TicketForm.tsx` — react-hook-form + zod, create and edit
20. `SubTicketList.tsx`, `SubTicketRow.tsx`, `SubTicketForm.tsx`
21. `ProgressBar.tsx`, `ActivityTimeline.tsx`, `CommentInput.tsx`, `OverdueBanner.tsx`

#### Ticket feature views

22. `TicketsListView.tsx` — role-scoped list
23. `TicketCreateView.tsx` — team leader only
24. `TicketDetailView.tsx` — full detail with activity + sub-tickets

### Phase 4: Routing & Wiring

- Add routes: `/tickets`, `/tickets/new`, `/tickets/:id`

---

## Plan 9 — User-as-Hub Firestore Architecture

**File:** `we-have-an-issue-expressive-dream.md`

# Plan: User-as-Hub Firestore Architecture

## Context

Two problems:

1. **Missing reverse index**: `users/{uid}` only has `teamId: string | null` (single team). `Team.employees` is a map so Firestore can't query "teams where user X is a member". Result: show all teams or nothing.
2. **Flat permission model**: No per-user, per-feature or per-entity permission control stored directly on the user doc.

## What Gets Added to `users/{uid}`

```json
{
  "teamIds": ["team1"],
  "assignedTicketIds": ["t1","t2"],
  "assignedSprintIds": ["s1"],
  "createdBy": "adminUid",
  "teamAddedBy": "adminUid",
  "features": { "summary": true, "timeline": false, "kanban": true, ... },
  "workflowPermissions": [{ "id": "statusId", "canView": true, "canEdit": false, "canDelete": false }],
  "teamPermissions": [],
  "epicPermissions": [],
  "labelPermissions": [],
  "sprintPermissions": []
}
```

## Changes by File

### `src/features/auth/models/AuthUser.ts`

- Add `FeatureKey`, `UserFeatures`, `DEFAULT_FEATURES`, `EntityPermission`, `EntityPermissionType` types
- Update `AuthUser` interface: remove `managerId`, add all new fields
- `authUserFromDoc`: read new fields with fallback defaults

### `src/features/auth/repo/AuthRepo.ts`

Add: `getUser`, `setUserFeatures`, `setEntityPermissions`

### `src/features/teams/repo_impl/TeamRepoImpl.ts`

Add private helpers `addTeamToUser` / `removeTeamFromUser`. Wire into createTeam, deleteTeam, addMember, removeMember, setTeamLeader.

### `src/features/tickets/repo_impl/TicketRepoImpl.ts`

Add private helpers `addTicketToUsers` / `removeTicketFromUsers`. Wire into createTicket, updateTicket (when assigneeIds changes), softDeleteTicket.

### `src/features/sprints/repo_impl/SprintRepoImpl.ts`

Add private helpers `addSprintToUsers` / `removeSprintFromUsers`. Wire into addTicketToSprint, removeTicketFromSprint, deleteSprint.

## Build Order

1. `AuthUser` model (types + mappers)
2. `AuthRepo` interface
3. `AuthRepoImpl`
4. `TeamRepo` interface
5. `TeamRepoImpl`
6. `TicketRepoImpl`
7. `SprintRepoImpl`
8. Permission helpers in `src/lib/permissions.ts`

---

## Plan 10 — Permission-Driven Access Control (End to End)

**File:** `steady-roaming-tome.md`

# Permission-Driven Access Control — End to End

## Root cause (confirmed by tracing the code)

There are **two mismatched authorization systems**:

1. **App layer** (`Role.ts`) defines only `pending` and `admin`. Assigning a custom role writes **only** `customRoleId` and leaves base `role: 'pending'`.
2. **Firestore rules** still key off long-removed roles (`member`, `team_leader`, `manager`) and gate everything on `isActive() = role != 'pending'`.

Because a custom-role user stays `pending`:

- `usePermissions()` → `getCustomRole()` reads `/roles/{id}`, whose rule is `allow read: if isActive()` → **denied** → custom role never loads → grants resolve to empty `pending` set → only Profile/About render.

## Agreed approach

- **Hide** disallowed controls entirely (no disabled state)
- **Full scoped enforcement** (UI + Firestore rules)
- **Permissions stay on the `roles` collection** — single source of truth, no denormalization onto user docs
- **Relax the `/roles/{id}` read rule** so any authenticated user can read their assigned role
- A custom-role user becomes active via an **`active: true`** flag set on the user doc at assignment time
- Model/effort: **Opus 4.8, high** — careful, wide-blast-radius, multi-session

---

## Phase 0 — Foundation & unblock

1. **`firestore.rules`** — change `/roles/{id}` read to `allow read: if authed()`. Fixes "only Profile shows."
2. **`AuthUser.ts`** — add `active: boolean`; update `authUserFromDoc` / `authUserToDoc` (default `false`; admin ⇒ implicitly active)
3. **`usePermissions.ts`** — keep `getCustomRole(user.customRoleId)` query (now succeeds)
4. **`AuthRepoImpl`** — `setUserCustomRole(uid, customRoleId)` → also set `active: true`; removal → `active: false`
5. **`firestore.rules` full rewrite** — `isActive()` = `authed() && (isAdmin() || userDoc().active == true)`. Remove `manager`/`team_leader`/`member` helpers.

## Phase 0b (next pass, lockout-safe)

Persist a `permissionScopes` map on each role doc; switch data-collection rules to per-permission `hasPerm`/`scopeOk` checks. Gate behind Firestore emulator rules tests before shipping.

## Phase 1 — Nav + route correctness

- **`AppSidebar.tsx`** — change `NavItem.permission?: Permission` to `anyOf?: Permission[]`; populate each item with the **same** permission set its route guard uses
- **`AppRoutes.tsx`** — reconcile each `PermissionGuard anyOf` with the sidebar set; `DefaultRedirect` sends user to their first accessible route
- Create `src/routes/navAccess.ts` (`NAV_ACCESS` + `LANDING_ORDER`) as single source of truth

## Phase 2 — Per-feature control gating

Pattern: gate each actionable control with `can(<permission>)` (and scope where entity ID is known). Apply across:

- Tickets: create, edit, soft-delete, move/status change, assign, comment, reply, react, subtask
- Board: drag/move gated by `can_move_*`
- Epics: create/edit/remove/assign-teams/add-remove-tickets
- Sprints: create/edit/delete/add-remove tickets
- Teams: manage teams, manage members
- Admin: `can_edit_roles`, `can_manage_statuses`, `can_manage_labels`

## Phase 3 — Scoped data filtering

Add `useScopedFilters()` helper (in `auth/logic`) that turns the user's grants into query scope.

- Tickets: if `can_view_all_tickets` → no scope filter. Else build union of: own team, custom-team scopeIds, custom-epic, custom-label, custom-user, own (`createdBy == uid`), assigned (`assigneeIds contains uid`). Run minimal constrained queries, merge/dedupe in repo.
- Epics / Sprints / Teams: apply team scope analogously.

## Phase 4 — Tests & verification

- Unit: `effectiveGrants` (admin ⇒ all; custom role ⇒ its grants); `can()`/scoped `can(key,id)`; `useScopedFilters` output for grant combinations
- Rules: Firestore emulator tests (`@firebase/rules-unit-testing`); cover: active flag gates reads; scoped grant allows only in-scope docs; admin bypasses; pending denied
- Manual E2E: assign custom role → user sees exactly those tabs; direct URL to guarded route redirects; Firestore denies out-of-scope reads

## Phase 0 vs 0b sequencing note

Role permissions persist as an array of `{key, scopeIds}` — Firestore rules cannot filter an array of objects by field, so per-permission scoped rule checks require a **map** form (`{key: scopeIds}`) on the role doc, i.e. a data migration. Phase 0 uses active-flag based rules; Phase 0b migrates to full permission/scope-aware rules.

## Progress log (all phases completed ✅)

- ✅ Phase 0 — `active` flag, `/roles` read relaxed, rules rewritten
- ✅ Phase 1 — `navAccess.ts`, AppSidebar, AppRoutes, DefaultRedirect
- ✅ Phase 0b — `permissionScopes` map, `migrateRolePermissionScopes`, 16 emulator rules tests
- ✅ Phase 2 — action controls gated, `DraggableCard.disabled`, `own_team_*` variants
- ✅ Phase 3 — `deriveTicketViewScope`, `useTicketViewScope`, `listTicketsScoped`, wired into `TicketsListView`

---

## Plan 11 — SaaS Transformation + Landing Page

**File:** `look-i-need-to-glittery-torvalds.md`

# SaaS Transformation Plan — HelpDesk Lite

## Context

Convert HelpDesk Lite from a single-company internal tool (admin hardcoded via env/config) into a SaaS product where anyone can sign up and become an admin of their own workspace. Also add a public landing page with luxury/minimal gen-z aesthetics.

## Part 1 — SaaS Auth Model

**New rule:** Every signup → `role: 'admin'`, `active: true`, `createdBy: null` in Firestore `users` doc. No env variable, no config file.

**Changes:**

1. **`AuthRepoImpl.ts`** — `signUp()` always writes `role: 'admin'`, `active: true`. Remove `adminConfig` import.
2. **`adminConfig.ts`** — delete

## Part 2 — Signup View

**Files to create:**

- `src/features/auth/ui/views/SignupView.tsx` — displayName + email + password + confirm password

**Files to modify:**

- `src/routes/AppRoutes.tsx` — add `/signup` route
- `src/features/auth/ui/views/LoginView.tsx` — add "Don't have an account? Sign up" link

## Part 3 — Landing Page

### Route

`/` becomes the landing page (public, no auth). Move `DefaultRedirect` to `/app`.

### Landing page sections (Tailwind theme tokens only + CSS custom keyframes, no new libraries)

**Hero** — full-viewport, dark navy (`bg-primary`). Large headline with gradient text (gold accent). Two CTAs: "Get started free" → `/signup`, "Sign in" → `/login`. Floating card mockup with CSS transforms + float animation.

**Features strip** — 3 columns: Ticket Management, Team Collaboration, Role-Based Access. Cards use `bg-surface`, `shadow-card`, `rounded-card`.

**How it works** — numbered steps: Sign up → Invite team → Track tickets.

**Social proof** — "500+ teams", "10k tickets resolved". Glassmorphism card with slight blur.

**CTA footer band** — dark, gold accent button, tagline.

### Animations (CSS only)

- Hero headline: `@keyframes hd-fade-up` in `index.css`
- Feature cards: staggered `animation-delay`
- Floating mockup: `@keyframes hd-float` (translateY loop)
- Scroll-triggered: `IntersectionObserver` in a `useInView` hook (inline, no lib)

## Part 4 — AuthGuard redirect fix

Authenticated users hitting `/` (landing) redirect to `/app` — small check in `LandingView` using auth Redux state.

## Build order

1. Delete / gut `adminConfig.ts`, update `AuthRepoImpl.signUp()` → admin role
2. Add `SignupView`, wire `/signup` route, link from `LoginView`
3. Add landing CSS keyframes to `index.css`
4. Build `LandingView` with all sections
5. Rewire `/` route in `AppRoutes.tsx`, move `DefaultRedirect` to `/app`
